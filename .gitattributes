# Normalize all text to LF in the repo (checkout native via eol where needed).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.icns binary
*.db binary
*.woff binary
*.woff2 binary
